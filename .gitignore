# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
mindshare_submissions.csv
mindshare_submissions_3.csv

# Runtime data is server-owned, never versioned. Server is source of truth;
# copy down with rsync when a local snapshot is needed. See data/README.md.

# Legacy root-level data dumps (contain user PII — also purge from history)
state.json
leaderboard.csv
leaderboard_export.csv
epoch2_leaderboard.csv
epoch2_top2.csv
epoch2_mindshare_data.zip

# Waitlist durable files (JSON default + optional CSV mirror)
waitlist_state.json
data/waitlist/state.json
data/waitlist/csv/

# Epoch 2 production snapshots (built on server; never commit)
data/newmindshare/*
!data/newmindshare/.gitkeep
!data/newmindshare/README.md

# Mindshare runtime/cache/snapshots/backups — server-owned, never commit
data/mindshare/*
!data/mindshare/.gitkeep

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

